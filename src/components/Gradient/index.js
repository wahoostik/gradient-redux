import React from 'react';

import './styles.scss';

const Gradient = () => (
  <div
    className="gradient"
    style={{ background: 'linear-gradient(45deg, #f0f, #48b1f3)' }}
  />
);

export default Gradient;
